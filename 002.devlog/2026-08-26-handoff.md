# 项目交接文档 (Handoff) — 2026-08-26

## 项目：Miss Player WebDAV 多端 CRDT 同步与 Safari 深度体验稳定性攻坚

---

## 1. 当前总体状态概览

当前主分支代码已完成 **v5.6.6** 正式版本的全量构建、测试与 GitHub 提交，所有核心功能均处于稳定可用状态：

| 模块 | 当前状态 | 核心实现与机制 |
| :--- | :---: | :--- |
| **WebDAV CRDT 同步引擎** | ✅ **100% 稳定** | 基于 Schema v2、30天墓碑机制（Tombstone）与字段级 LWW 合并，支持合并/上传/下载覆盖。 |
| **多端实时无感热更新** | ✅ **100% 稳定** | 后台 25 秒轻量心跳轮询 (`startPeriodicSync`) 配合 `mp_sync_applied` 事件实时刷新进度条打点。 |
| **多形态布局隔离** | ✅ **100% 稳定** | `desktop` / `mobile` / `tablet` 独立保存侧边栏方向与隐藏状态，跨端配置互不干扰。 |
| **Safari 弹窗广告拦截** | ✅ **100% 稳定** | 移除浮钮中的宿主 Plyr 按钮点击；`document-start` 极早劫持 `window.open` 与 `<a>.click`。 |
| **Safari 内联播放保护** | ✅ **100% 稳定** | 永久锁定 `<video>` 的 `playsinline` / `playsInline=true` 属性，彻底解决二次打开跳系统全屏问题。 |
| **UI 三维层级与交互** | ✅ **100% 稳定** | 设置面板 `z-index: 100000` 提权；悬浮模式视频点击播放/暂停流畅同步。 |

---

## 2. 核心架构与关键文件拓扑

```text
src/
├── sync/
│   ├── SyncManager.js            # [CRDT引擎] 收集本地数据、Schema迁移、墓碑过滤、25s轮询与持久化
│   ├── WebDavClient.js           # [协议客户端] 极简直连 GET/PUT、404/409目录补建、12s超时熔断与onabort
│   └── index.js                  # 模块统一入口
├── player/
│   ├── ui/
│   │   ├── FloatingButton.js     # [浮钮] 用户手势栈直调 video.play()，杜绝宿主 DOM 点击
│   │   ├── UIManager.js          # [界面管理] 挂载时强制 playsinline 与 webkitExitFullscreen 守卫
│   │   └── styles/
│   │       └── _player.css       # 设置面板 z-index: 100000，状态徽标 (.running/.success/.error)
│   ├── core/
│   │   └── PlayerCore.js         # [核心] 退出时保持 playsinline 属性，防止 Safari 触发原生全屏
│   └── managers/
│       ├── BlurPlaybackManager.js # [失焦管理] 响应 pauseOnBlur 开关，支持背景音频播放
│       └── LoopManager.js        # [打点] 监听 mp_sync_applied 实现多端实时热刷新
├── adblock/
│   └── index.js                  # [广告拦截] document-start 极早注入，劫持 window.open 与 a.click
└── constants/
    └── domains.js                # 多站点域名矩阵 (MissAV, Jable, JavDB, JavLibrary)
```

---

## 3. 关键架构规范与维护红线 (必读)

1. **Userscript 元数据绝对红线**：
   - `@namespace` 必须严格固定为 `loadingi.local`，严禁修改；
   - `@name` 必须保持为 `Miss Player | 影院模式 (单手播放器)`。
2. **三处版本号同步规则**：
   - 当前版本为 **`5.6.6`**；每次发版必须且只能同步递增：
     1. `package.json` 中的 `version`
     2. `webpack.config.js` 中的 `headers.version`
     3. `src/telemetry/EventCollector.js` 中的 `getScriptVersion()` fallback
3. **移动端 Safari 内联播放红线**：
   - 严禁从 `<video>` 上移除 `playsinline` 或将 `video.playsInline` 置为 `false`；
   - 严禁在用户手势调用栈中触发宿主原站包含广告劫持的 DOM 元素（如 `.plyr__control--overlaid`）。
4. **WebDAV 请求安全规则**：
   - 所有跨域请求必须设置严格超时（<= 12s），且必须捕获 `onabort` 与异常，严禁无 Body 的裸 `PROPFIND`。

---

## 4. 后续规划与演进路线 (Next Steps)

1. **WebDAV 历史版本快照回滚**：
   - 在 WebDAV 备份目录中保留近 3 次同步历史快照（`miss_player_sync_backup_1.json`），支持一键历史还原；
2. **多站点播放流提取器（Extractors）深度迁移**：
   - 将成熟的 `7mmtv.sx`（iframe 穿透）与 `Jav.sb`（M3U8 坏片净化）流提取器合并入播放器原生内核；
3. **CRDT 性能与大体积存储优化**：
   - 当打点视频数量超过 1000 部时，建立打点按视频维度的增量分片同步机制。
