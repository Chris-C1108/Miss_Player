 开发日记 — 2026年7月22日
## 油猴播放器设置菜单重构、快进/快退步进流式横向滚动与自定义扩展、侧边栏显示联动控制及评论源静默预加载防漏拦截

---

## 1. 核心需求与实现功能

### 1.1 `tm-settings-panel` 视觉与交互全量重构
* **毛玻璃磨砂质感与现代化卡片**：
  * 使用高模糊度 `backdrop-filter: blur(20px)`，结合 `border-radius: 14px` 与深邃软阴影 (`box-shadow: 0 16px 40px rgba(0, 0, 0, 0.6)`)，打造符合现代极简与 Glassmorphism 美学的面板外观。
  * 修复了 `<div class="tm-settings-panel">` 之前被硬编码 `style.display = 'none'` 导致 CSS 渐变淡入动画 (`opacity` + `transform`) 失效的问题。
* **主次字体排版与精细分割线**：
  * **次级大写分区标题** (`遥控器 :`, `评论区 :`, `其他 :`)：字号设为 `11px`，配合 `font-weight: 700`、`letter-spacing: 0.8px`、`text-transform: uppercase` 与低饱和度淡色，建立清晰的层级架构。
  * **主级选项名称** (`进度条栏`, `快进/快退栏`, `跳转/循环栏`, `是否展示评论区`, `DEBUG`)：字号升级为 `13.5px`，使用 `font-weight: 500` 与纯白高亮文本。
  * **渐变分割线**：采用 1px 线性渐变下划线 (`linear-gradient(90deg, hsla(...) 0%, hsla(...) 100%)`)，替代粗糙的固体线边框。

### 1.2 遥控器快进/快退步进流式横向无缝滑动与自定义扩展
* **对称步进设计与动态解析**：
  * 步进控制改为单向对称配置模式，在 `SeekController.js` 中新增 `parseStepToSeconds(stepKey)` 动态解析工具，支持 `5s`, `10s`, `30s`, `1m`, `5m`, `10m` 等格式并按时间升序自动排序。
* **流式横向滑动 (Horizontal Scroll Subpanel)**：
  * 重构 `tm-settings-seek-steps-subpanel` 样式，采用 `flex-wrap: nowrap; overflow-x: auto; white-space: nowrap;` 设计，结合 `scrollbar-width: none` 隐藏滚动条。
  * 面板默认宽度（`width: 310px`）精巧支持展示 6 个预设步进胶囊按钮，第 7 个 `+`（自定义步进按钮）藏于右侧可视区外，用户向左滑动即可自然发现。
  * **PC 端滚轮转横向滚动**：在 `SettingsManager.js` 中为子面板绑定 `wheel` 事件，将鼠标竖向滚轮位移自动映射为 `scrollLeft` 横向位移。
* **自定义步进增删与长按交互**：
  * 点击 `+` 按钮即时替换为文本框，支持填写格式为 `_s` 或 `_m`（如 `15s`, `2m`），正则校验后保存至 `customUserSeekSteps` 本地存储并默认激活。
  * **长按删除机制**：长按自定义胶囊按键 600ms 触发提示与删除逻辑，默认 6 个预设步进按键保护不可删除。
* **参考 `tm-time-control-button` 胶囊按键质感**：
  * 设置 `.tm-seek-step-badge` 与 `.tm-source-badge` 的胶囊外形（高度 28px、圆角 6px、`display: inline-flex; align-items: center; justify-content: center; line-height: 1;`），实现文字与容器像素级垂直居中；配合悬浮上浮（`translateY(-1px)`）与按压微缩（`scale(0.94)`）微交互。

### 1.3 侧边栏（评论区）显隐控制与全局按钮联动
* **隐藏/恢复联动**：
  * 当在设置中关闭“是否展示评论区” (`showCommentsSection: false`) 时，侧边栏面板完全隐藏，播放器容器添加 `.tm-sidebar-hidden` 类。
  * **控制按钮同步隐藏**：在 `UIManager.js` 中增加 `updateSidebarButtonsVisibility()` 方法，关闭评论区时，控制栏上的 `tm-sidebar-pos-button`（侧栏位置切换）与 `tm-sidebar-toggle-button`（侧栏显隐切换）自动同步隐退 (`display: 'none'`)。
* **关转开配置自动重置**：
  * 当用户从关闭状态重新开启“是否展示评论区”时，自动重置用户之前的隐藏配置 (`sidebarHidden = false`)，并实时刷新侧栏切换按钮的图标与状态，确保评论区能够立刻恢复并呈现在用户面前。

### 1.4 评论源 Badge 动态控制与按需自动采集
* **Section 动态显隐控制**：
  * 在 `CommentPanel.js` 中新增 `updateCommentSources()` 与 `renderSectionHtml(siteKey)`，根据 `enabledCommentSources` 的布尔值动态渲染/隐藏 `#tm-comment-section-jable`、`#tm-comment-section-javdb`、`#tm-comment-section-javlib`。
* **按需自动触发抓取 (Lazy Auto Fetch)**：
  * 初始加载时仅抓取开启的站点评论，跳过已禁用的站点。
  * 当用户在设置面板中首次点击勾选开启某个原本关掉的站点（如勾选启用 `Javlibrary`）时，若该站点尚未抓取过评论，系统自动触发 `loadSiteComments(siteKey, 1)` 进行抓取。

### 1.5 跨域与预加载设置防漏校验 (Preload Guard Fix)
* **前置预加载守卫**：
  * 修复了脚本在 Boot 阶段 (`index.js` 调用 `CommentPanel.preload(videoCode)`) 无视用户设置、强行并发发起 3 个站点跨域预加载请求的问题。
  * 在 `CommentPanel.preload` 与 `loadSiteComments` 入口处加上严格的防漏校验：若 `showCommentsSection` 为 `false` 或特定站点源为 `false`，彻底跳过与拦截 HTTP 请求。

---

## 2. 踩坑教训与避坑经验

### 2.1 状态持久化键名不匹配导致的“全局设置失效”幽灵 Bug
* **坑点**：在重构预加载防漏逻辑时，在 `CommentPanel.preload` 中尝试使用 `GM_getValue('tm_player_settings')` 读取配置对象。然而底层 `PlayerState.js` 实际采用的是 `getValue('showCommentsSection', true)` 逐个键名独立存储的方式（在 GM 存储中键名为 `showCommentsSection` / `enabledCommentSources`，而不是一个统一的 JSON 对象 `tm_player_settings`）。这导致 `preload` 方法读到的永远是 `null`，后端的拦截守卫完全失效，控制台中依然看到脚本去采集所有站点的评论。
* **经验**：在非面向对象实例的静态方法或外部模块中读取存储状态时，**必须与系统全局存储架构（`PlayerState` / `storage.js`）的键名规范及存储模式保持 100% 严格一致**。涉及 GM 存储与 localStorage 混合降级时，应当优先封装或调用统一的存储工具函数，切忌凭主观猜测凭空硬编码键名。

### 2.2 多层级并发/预加载逻辑的“防御性配置校验”
* **坑点**：为了优化首屏加载速度，系统往往会在主程序初始化前（Boot Time）提前触发后台静默预加载（如 `CommentPanel.preload`）。但如果预加载逻辑独立于常规 UI 渲染流程之外，且没有在触发的最顶端校验用户持久化设置，就会导致用户明明在 UI 里禁用了某功能，后台网络请求依然大张旗鼓发出的怪异现象。
* **经验**：任何异步预加载、后台静默请求或 Hook 模块，在执行耗时/跨域网络操作的第一行代码处，都必须添加“防御性配置校验（Defensive Configuration Check）”，保证用户的个性化设置在系统的每一个角落（包括最前置的 Boot Preload）都能生效。

### 2.3 软键盘/触摸屏下的流式无缝横向滚动（No-wrap Flex Horizontal Scroll）
* **坑点**：在 Modal/Popover 等小尺寸弹窗内嵌入横向滚动条时，如果简单使用 `overflow-x: auto`，常常会因为 Flex 子元素的收缩机制（`flex-shrink: 1`）导致胶囊按钮被挤压变形，或者因为没有处理 `white-space` 而在小屏设备上发生换行破版。
* **经验**：实现干净的无缝流式胶囊横向滚动三要素：
  1. 父容器设置 `flex-wrap: nowrap; overflow-x: auto; white-space: nowrap; -webkit-overflow-scrolling: touch;`
  2. 隐藏原生粗糙滚动条 (`scrollbar-width: none; ::-webkit-scrollbar { display: none; }`)
  3. 子元素必须显示声明 **`flex-shrink: 0;`**，防止内部控件在空间不足时遭弹性挤压；同时针对桌面端绑定 `wheel` 事件将竖向滚轮位移映射至 `scrollLeft`。

### 2.4 Flex 容器下文本与胶囊按钮的像素级垂直居中
* **坑点**：设置 `height` 与 `line-height` 在不同浏览器或字体渲染引擎下，经常因字体 Metrics（Baseline / Ascender / Descender）的微小差异导致文本上下偏离 1~2px。
* **经验**：避免单纯依赖 `line-height` 进行垂直居中。对于按钮、徽章或胶囊组件，推荐使用 **`display: inline-flex; align-items: center; justify-content: center; line-height: 1;`**，配合显式 `height` 与 `padding`，能确保跨平台下的绝对像素级垂直居中。

---

## 3. 统计与构建验证

* **修改与重构文件**：
  * [src/player/managers/SettingsManager.js](file:///d:/01_Development/Miss_Player/src/player/managers/SettingsManager.js)
  * [src/player/controls/CommentPanel.js](file:///d:/01_Development/Miss_Player/src/player/controls/CommentPanel.js)
  * [src/player/controls/SeekController.js](file:///d:/01_Development/Miss_Player/src/player/controls/SeekController.js)
  * [src/player/ui/UIManager.js](file:///d:/01_Development/Miss_Player/src/player/ui/UIManager.js)
  * [src/player/state/PlayerState.js](file:///d:/01_Development/Miss_Player/src/player/state/PlayerState.js)
  * [src/player/ui/styles/_player.css](file:///d:/01_Development/Miss_Player/src/player/ui/styles/_player.css)
* **构建验证**：Webpack 5 (`npm run build`) 顺利通过打包编译，产物 [dist/miss_player.user.js](file:///d:/01_Development/Miss_Player/dist/miss_player.user.js) 运行正常，0 报错。

---

## 4. 移动端横屏布局重构、控制面板智能隐退、Minimap 联动与评论边栏 Flex/Grid 视口溢出修复

### 4.1 核心需求与实现功能

#### 1. 移动端横屏布局触发条件全量解绑
* **解除硬编码高度限制**：
  * 彻底移除以往 CSS 与 JS 中针对横屏模式 `min-height: 400px` 与 `max-height: 399px` 的判定分支。
  * 无论移动设备横屏高度为 360px、399px 还是 430px，均统一样式触发条件（仅判断 `orientation: landscape` 与 `min-width: 930px`），使得小尺寸横屏设备（如 iPhone 14 Pro Max 932×430 等）均能精准展示“侧边栏 + 小控制面板”与“仅小控制面板”响应式布局。

#### 2. 横屏悬浮与吸附控制面板下的全量自动隐藏与顶栏标题联动
* **无操作 3 秒自动隐退**：在 `UIManager.js` 中优化 `autoHideControls()` 倒计时逻辑，移除对控制面板吸附状态 (`isDocked`) 的提前拦截阻断。无论控制面板处于悬浮状态还是吸附停靠在侧边栏底部，无操作 3 秒后均正常触发 `hideControls()`。
* **点击与滑动即刻隐藏**：
  * 点击视频画面区域或手势滑动平移画面时，立即调用 `hideControls()` 触发全局 `body.controls-hidden` 状态。
* **顶栏视频标题 (`.tm-player-title`) 自动淡出**：
  * 触发 `body.controls-hidden` 时，视频上方覆盖的视频标题 (`.tm-player-title`) 自动向上平滑淡出隐藏（`opacity: 0; transform: translate(-50%, -20px);`）。
  * 配合 CSS 规则（`body.controls-hidden .tm-player-container[class*="tm-controls-docked-"] .tm-control-buttons`），吸附在侧边栏底部的控制面板保持在侧边栏内部常驻可见，而画面区上方的标题与覆盖件则在 3 秒后优雅隐退，交互与视觉体验完美一致。

#### 3. 画面左右平移 Minimap 微缩图按需淡入、渐隐与非标准比例溢出修复
* **彻底默认隐退与手势平移即时淡入**：
  * 将 `.tm-video-minimap` 的默认 CSS 状态设为 `opacity: 0 !important; visibility: hidden !important;`，在常态播放或静止时彻底隐藏，解决以往 Minimap 无脑常驻显示的问题。
  * 仅在检测到手势水平平移 (`.is-swiping`) 时，通过 CSS 淡入高亮显示 (`opacity: 1; visibility: visible; border-color: rgba(...)`)。
  * 手势平移结束释放后，JS 延迟 600ms 移除 `.is-swiping` 类，结合 `transition: opacity 0.3s ease, visibility 0.3s ease` 实现自然平滑的淡出隐退。
* **非标准宽高比 (1:1 / 9:16) 下的 Minimap 尺寸自适应与边界防溢出**：
  * 重构 JS 中 Minimap 动态宽高计算逻辑。以往直接按 `80 / ratio` 计算高度，当视频为 1:1 正方形 (ratio=1) 时，高度被计算为 80px，导致 Minimap 变成 80x80px 的巨大方块，在移动端横屏 short viewport 下被底部边界截断或挤出画面。
  * 现采用最大宽高上限约束策略（宽上限 80px，高上限 45px）：
    ```javascript
    const maxW = 80, maxH = 45;
    let miniW = maxW, miniH = Math.round(maxW / ratio);
    if (miniH > maxH) {
        miniH = maxH;
        miniW = Math.round(maxH * ratio);
    }
    ```
    对于 1:1 正方形视频，Minimap 尺寸自动约束为 45x45px；对于 16:9 视频约束为 80x45px，保证在任何屏幕尺寸和视频比例下均精准位于 `.tm-video-wrapper` 左下角 (`bottom: 20px; left: 20px;`)，绝不溢出截断。
* **零重排 GPU 加速与 100% 实时跟手 (Zero-Reflow GPU Acceleration)**：
  * **消除 `getBoundingClientRect()` 布局抖动**：以往在手势 `pointermove` 频率高达 120Hz 的事件处理器中，每次拖拽都强行调用 `video.getBoundingClientRect()` 触发 DOM 布局同步重排 (Layout Thrashing)，导致移动端拖拽极度卡顿、延时不跟手。
  * **预计算行程与 `transform: translateX` 替代 `left`**：在 `_updateConstraints()` 阶段提前计算并固化 Minimap Viewport 的最大行程 (`minimapMaxTravel = miniW - viewportW`)。在 `_applyOffset` 中直接映射计算 `miniX` 并通过 **`minimapViewport.style.transform = translateX(${miniX}px)`** 提交给 GPU 合成线程渲染，同时移除 CSS `transition` 延迟，实现 120fps 绝对零延迟丝滑跟手。
* **移动端横屏安全区与避让位移**：
  * 在 `_landscape_pc.css` 中将 Minimap 在横屏下的位置调整为 `bottom: calc(36px + env(safe-area-inset-bottom, 0px)) !important; left: calc(24px + env(safe-area-inset-left, 0px)) !important;`，完美抬高 16px 避开移动设备底部的 Home 条状按键与下方视口边缘遮挡，确保整个 Minimap 100% 完全呈现在视频区域内。

#### 4. 评论区边栏视口高度溢出与底部栏遮挡重构
* **CSS Grid Track 高度收缩修复**：修复了 `.tm-player-container` 之前采用 `grid-template-rows: 1fr` 导致 Grid Row 按照内容 `min-content` 无限撑高的缺陷，改用 `grid-template-rows: minmax(0, 1fr) !important;` 严格约束 Grid 单元格不超过视口高度。
* **WebKit Flex 继承高度坍塌与零值约束**：
  * 在 WebKit（Safari / 移动端 Chrome DevTools）渲染引擎下，嵌套 Flex 子元素若未显式指定 `height: 0` 与 `min-height: 0`，会递归继承评论卡片的总高度（400px+），从而将评论面板底部的 `.tm-comment-submit-bar-wrapper` 与 `.tm-comments-panel-action-bar` 挤出屏幕下方。
  * 在 `_landscape_pc.css` 中为 `.tm-comments-panel-list`、`.tm-comment-section` 和 `.tm-comment-section-body` 统一补充 `height: 0 !important; flex: 1 1 0% !important; min-height: 0 !important;`，并为底部栏指定 `flex-shrink: 0 !important;`，使评论列表在剩余空间内独立滚动，底部工具栏永久固定在视口下方。

#### 5. `tm-comments-panel-action-bar` 显隐联动 `debugMode`
* **设置项初始化同步**：
  * 在 `CommentPanel.js` 模版生成中给 `.tm-comments-panel-action-bar` 默认添加 `style="display: none;"`。
  * 在 DOM 创建完成后立即调用 `this.applySettingsState()`，使得该底部功能栏在 `"debugMode": false` 时静默隐藏不占空间，仅在用户开启 DEBUG 模式时动态渲染 `display: flex;`。

---

### 4.2 踩坑教训与避坑经验

#### 1. CSS Grid 规范中 `1fr` 隐藏的 `min-content` 撑高陷阱
* **坑点**：在 CSS Grid 布局中，习惯性给网格行设置 `grid-template-rows: 1fr`。但在 W3C CSS Grid 规范中，`1fr` 等价于 `minmax(auto, 1fr)`！当网格项（如评论边栏）内部包含非固定高度的 Flex 溢出内容时，`auto` 最小尺寸会以子元素的 `min-content` 高度（例如 524px）计算，直接导致 Grid 行的高度超越容器视口（430px），进而引发页面滚轴破版或底部固定栏溢出消失。
* **经验**：在需要严格限制 Grid 单元格高度等于视口/容器高度的场景下，**切勿直接使用 `1fr`**，必须显式书写为 **`grid-template-rows: minmax(0, 1fr)`**，明确告知浏览器允许该行高度收缩至 `0`。

#### 2. WebKit / Safari 引擎下多层 Flex 嵌套的 `height: 0` 必填法则
* **坑点**：仅给最外层 Flex 容器设置 `overflow: hidden` 与 `min-height: 0`，但在深层子级 Flex 元素（如列表项 Section、Section Body）中只写了 `flex: 1`。在 Blink/Gecko 上可能表现正常，但在 iOS WebKit 引擎中，未设 `height: 0` 的中间 Flex 子项仍会按内容实际高度展开，破坏最上层容器的 `flex-shrink` 计算。
* **经验**：在包含内部独立滚动（`overflow-y: auto`）的多层 Flex 布局中，所有参与高度分配的弹性子元素（自上而下每一级）都应当同时声明：
  ```css
  flex: 1 1 0% !important;
  height: 0 !important;
  min-height: 0 !important;
  ```
  结合底栏控件的 `flex-shrink: 0 !important;`，才能确保跨浏览器、跨平台表现 100% 一致。

#### 3. JS 内联 Style (`element.style.display`) 与 CSS 状态类 (`.is-swiping`) 冲突解耦
* **坑点**：以前在手势管理者（如 `videoSwipeManager`）中喜欢用 `element.style.display = 'block'` / `'none'` 强行控制 UI 控件（如 Minimap）的显隐。一旦 JS 写入内联 style，其优先级高于外部绝大多数 CSS Class 规则，导致后期想要通过 CSS 状态类（如 `body.controls-hidden` 或 `.is-swiping`）控制过渡动画与显隐逻辑时被内联样式锁死，产生难以调试的“样式失效”问题。
* **经验**：UI 控件的显隐与动画过渡应**尽可能通过 DOM 状态 Class (`classList.add/remove`) 进行表达**，避免在 JS 中直接操作 `style.display` / `style.opacity`。将视觉呈现完全退归 CSS 管理，代码逻辑将更具拓展性与可维护性。

---

### 4.3 修改与重构文件汇总

* **修改文件**：
  * [src/player/managers/videoSwipeManager.js](file:///d:/01_Development/Miss_Player/src/player/managers/videoSwipeManager.js)
  * [src/player/controls/CommentPanel.js](file:///d:/01_Development/Miss_Player/src/player/controls/CommentPanel.js)
  * [src/player/ui/UIManager.js](file:///d:/01_Development/Miss_Player/src/player/ui/UIManager.js)
  * [src/player/ui/styles/_landscape_pc.css](file:///d:/01_Development/Miss_Player/src/player/ui/styles/_landscape_pc.css)
  * [src/player/ui/styles/_controls.css](file:///d:/01_Development/Miss_Player/src/player/ui/styles/_controls.css)
  * [src/player/ui/styles/_landscape_legacy.css](file:///d:/01_Development/Miss_Player/src/player/ui/styles/_landscape_legacy.css)
  * [src/player/ui/styles/_comments.css](file:///d:/01_Development/Miss_Player/src/player/ui/styles/_comments.css)
* **构建验证**：Webpack 5 (`npm run build`) 编译顺利通过，产物 [dist/miss_player.user.js](file:///d:/01_Development/Miss_Player/dist/miss_player.user.js) 验证无误。
