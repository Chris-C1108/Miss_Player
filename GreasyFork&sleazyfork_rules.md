# GreasyFork & SleazyFork 脚本合规开发与发布准则

> 本准则基于 [SleazyFork Code Rules](https://sleazyfork.org/zh-CN/help/code-rules) 与 [GreasyFork Code Rules](https://greasyfork.org/zh-CN/help/code-rules) 制定，是 Miss Player 项目在功能设计、代码构建与版本分发时必须严格遵守的最高合规标准。

---

## 1. 核心合规红线 (Top Red Lines)

1. **绝对禁止隐瞒负面功能 (No Undisclosed Antifeatures)**
   - 任何涉及用户行为追踪、数据回传、分析遥测（Telemetry）、广告植入或后台行为的代码，均属于平台定义的“负面功能”（Antifeature）。
   - **红线要求**：鉴于成人站点脚本对用户隐私的极端敏感性，**本项目执行 100% 纯本地运行原则，永久下线并禁止任何形式的网络遥测与指纹采集**。若确有特殊必要，必须在脚本元数据头强制声明 // @antifeature tracking <说明> 并默认关闭。

2. **绝对禁止代码混淆与压缩 (No Obfuscation / No Minification)**
   - 提交到平台上的代码必须保持完全透明与可审计。
   - 严禁启用 Terser 的变量重命名混淆（mangle: false）。
   - 严禁采用任何形式的 JS 加密、动态执行加密字符串（如 val(function(p,a,c,k...))）或混淆工具。
   - 构建产物必须保持清晰的代码排版、缩进及有意义的函数与变量命名。

3. **目标环境现代化，杜绝低版本 Polyfill 产生的隐形缩写**
   - 脚本面向运行在现代 Chromium / Firefox / Safari 上的用户脚本管理器。
   - 打包转译时禁止无差别降级到 ES5，必须将目标环境锁定为现代浏览器（如 Chrome 90+、Safari 14+），避免 Babel 注入携带单字母变量（, e, r, t, o）的辅助函数（如 _classCallCheck、_typeof），以免触发审计机器人的混淆误报。

4. **单文件体积硬红线**
   - 平台硬性限制单脚本文件大小不得超过 **2.0 MB**。
   - 构建产物必须严格监控体积，多媒体或大体积字体/图元资产必须采用 SVG 矢量化或外部按需 CDN，当前脚本安全线控制在 1.0 MB 以内。

---

## 2. 功能性合规标准 (Functionality)

* **描述与行为绝对一致**：脚本实际功能必须与元数据描述完全吻合，不得暗中执行未向用户明确告知的操作。
* **实质功能与独立性**：脚本必须具备实际增强功能，禁止发布仅提供网页重定向或超链接跳转的“空壳脚本”。
* **核心逻辑自包含**：脚本的主要逻辑必须直接包含在分发代码中，严禁在运行时从第三方服务器动态 val 或异步加载核心执行代码（规避审查行为会招致直接封号）。
* **禁止分发备用诱导链接**：不得在脚本或介绍页中引导用户下载未经验审的第三方“完整版”或外部包。

---

## 3. 元信息与清单规范 (Metadata & Listing)

* **严格受控的 @match 规则**：
  - 严禁为了虚增受众或匹配负面功能而盲目声明全局匹配。
  - 匹配规则必须精确对应脚本真正适配的域名矩阵（如 missav.ws、jable.tv 等）。
* **版本唯一性与发版同步**：
  - 严格保持 @namespace 为 loadingi.local，严禁变更主键；
  - 严格遵守发版版本号三处同步规则（package.json、webpack.config.js、内部 fallback）。
* **禁止 SEO 关键词堆砌**：
  - 脚本名称与简介中的多语言本地化翻译必须精准对应，禁止塞入大量无关热门站名、搜寻关键词或虚假营销词汇。
* **禁止恶意高频刷版**：
  - 不得以在首页刷榜为目的进行无实质内容的频繁版本更新。
  - 脚本内禁止实现高频自更新轮询，脚本的更新生命周期完全委托给用户的油猴扩展管理。

---

## 4. 依赖库与外部网络资源 (Dependencies & Networking)

* **第三方库引用规范**：
  - 首选通过 // @require <CDN-URL> 引入公共库；
  - 若采用 Webpack 内联打包第三方依赖（如 hls.js），必须在配置或模块头部保留原始库名、版本号与开源许可证声明。
* **网络请求白名单 (@connect)**：
  - 必须最小化声明 @connect 域名，仅保留跨域抓取评论和视频流所必需的目标站点域名；
  - 严禁声明未知的境外收集服务器或私有 API 分析端点。

---

## 5. 危机公关与违规处置应急预案 (Crisis & Incident Response)

* **收到违规举报时的红线纪律**：
  1. **代码先行**：严禁在未整改代码前去申诉区与管理员或举报者辩论；
  2. **先修复并发布**：本地彻底清除涉事问题代码，完成构建并推送到 GitHub 触发线上同步更新；
  3. **专业事实回复**：在申诉栏以客观、诚恳的英文详述“整改动作、修复版本号、清理范围”，申请关闭举报；
  4. **严禁二次发布被删脚本**：若脚本已被管理员下架，严禁作为新脚本重新上传（直接封号），必须通过既有举报/申诉通道申请恢复。
